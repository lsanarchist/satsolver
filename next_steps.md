# Next Steps

1. Use the upgraded profiler on the dense UNSAT-heavy pure-3-SAT bottlenecks (`large/test_6.cnf`, `special/hard.cnf`, `medium/test_4.cnf`) before changing heuristics again, and use `benchmark_suite.py --repeat 2` as the default confirmation pass for any change whose claimed gain is small enough that one-off full-suite timings could be noise.
2. Focus the next solver experiment on a more structural `propagate()` or conflict-analysis change, not another small bookkeeping tweak: immediate stale-watcher detachment, tuple-based blocker-literal watch entries that change watch-list payloads in pure Python, encoded watch-slot payloads that replace plain `clause_id` watcher entries, ternary-watcher splitting, mirrored ternary watch arrays, binary-path deleted-check removal, the `analyze()` touched-list shortcut, broad manual `analyze()` unrolling for `2..3`-literal reasons, lightweight cached best-variable hints for `pick_branch_literal()`, the old `backtrack()` local-caching cleanup branch, local `var_decay`/`clause_decay` tuning around `0.95/0.999`, repeated variable/sign multiplication in propagation, simple per-visit `len(lits)` caching inside `propagate()`, hot-loop `abs()` replacement with `literal_var[...]` indexing outside propagation, shadow clause-metadata arrays that duplicate `Clause` state without removing the objects, a cached `Clause.size` field by itself, full `Clause`-object removal into raw literal lists plus simple parallel metadata arrays, active trail-size indexing that leaves stale trail entries in place, extra learnt-list allocation during minimization, separate learnt-tail scans for backtrack-level plus LBD, watcher-list compaction that changes traversal order, and the already-known-unassigned `enqueue()` call overhead inside propagation are all now known dead ends or already-addressed costs on the hotspot slice. The new reverse-index-plus-tail-delete `backtrack()` rewrite is already in place and materially cheaper, and the tiny watched-clause `clause.learnt and clause.deleted` keep is now in place too, but both the profiler split and the workload composition still say the real remaining cost is raw original-clause ternary traversal. Future work should move back upstream into propagation/conflict analysis rather than stacking more micro-tweaks of similar size.
3. If clause minimization or `analyze()` is revisited, do not use a blunt global cutoff by reason-clause size or learnt-clause size, and do not jump straight to naive recursive redundancy traversal in pure Python. The upgraded profiler now says the hard UNSAT cases are driven overwhelmingly by ternary reasons, not binary ones: on `large/test_6.cnf`, `analyze()` traversals were `1,248` via size `2`, `930,068` via size `3`, `78,664` via `4..9`, and `47,866` via `10+`, while minimization removals were `12,293`, `100,492`, `27,850`, and `4,012` in those same buckets. `special/hard.cnf` and `medium/test_4.cnf` show the same shape, and SAT-like `large/test_8.cnf` had `0` binary versus `305,701` ternary traversals while `10+` reasons were still mostly kept (`19,467`) rather than removed (`2,856`). A hard reason-size cap still regressed badly, a narrow `len(learnt) <= 6` cutoff blew up SAT-like `large/test_8.cnf`, and recursive minimization also regressed the focused five-case serial slice from `30.0874s` to `35.9624s`. The new narrow binary/ternary fast path inside one-hop `minimize_learnt()` is already in place, the earlier out-of-tree ternary-only `analyze()` loop rewrite failed to clear the noise bar, and the newest original-clause-only ternary `analyze()` unroll also regressed slightly on the forward/reverse five-case average (`27.3915s -> 27.5393s`) while keeping identical decisions/conflicts/processed-literal counts on the main profiled cases. The positive counterexamples are now clearer: inlining the extremely hot `bump_var_activity()` call inside `analyze()` paid off, and so did deleting the touched-list cleanup entirely once token scoping made it redundant. But the newer helper-boundary rejects add three caution flags to that pattern: fully inlining `prepare_learnt_clause()` looked mildly positive on the module hotspot slice (`27.0032s -> 26.8062s`) and still lost the same-day exact-CLI repeat-aware 59-case suite (`27.1974s -> 28.2602s`), inlining `bump_clause_activity()` preserved identical conflicts on every hotspot case while still regressing the five-case average (`28.2330s -> 29.3646s`), and the newest per-variable inline problem-reason side arrays regressed much harder (`28.8895s -> 31.8809s`) while keeping the exact same conflicts on every hotspot case. So future conflict-analysis work should stay ternary-aware, favor helper-call and bookkeeping removal only when the removed cost is materially larger than learnt-clause activity bumping, avoid extra per-variable side-state that only replaces clause dereferences, and still require same-day exact-CLI confirmation before keeping small `analyze()` cleanups.
4. Do not use a global clause-retention rule based only on learnt size or a flat low-LBD threshold. Keeping all ternary learnt clauses helped the dense UNSAT four-case slice (`25.9122s -> 24.3444s`) but blew serial `large/test_8.cnf` up from `3.7121s` to `29.3577s`, and globally protecting `LBD<=3` clauses also lost badly overall by swelling the live learnt database (`large/test_8.cnf` `3.6933s -> 6.1171s`). The newest clause-family profiler split lowers the priority of broad learnt-retention work further on the main UNSAT bottlenecks: active ternary watch traffic there is about `95%..97%` original-clause work, while the active large-clause watch path is the learnt-clause side of the story. Future retention-policy work needs a much better classifier than clause size or a flat LBD threshold alone and should be checked specifically against SAT-like `large/test_8.cnf`.
5. Push the watched-clause path further only with changes that reduce actual relocation/unit/conflict work inside propagation without duplicating mutable watch state or changing traversal order too freely. The deeper profiler now says the hard UNSAT cases are dominated specifically by ternary work: on `large/test_6.cnf`, ternary outcomes account for `4.81M / 6.56M` relocations, `3.04M / 3.23M` units, and `53,776 / 59,197` watch conflicts, while large-clause replacement scans average only `2.36` probes (`3.61` on failure). `special/hard.cnf` and `medium/test_4.cnf` show the same shape. The newest clause-family split says that active ternary watch work on those same bottlenecks is also about `95%..97%` original-clause traffic, while active large-clause watch work is entirely learnt-clause traffic on these pure-3-SAT instances. The newest large-watch size split says that learnt-large traffic is not mostly 4-literal either: on the main hotspot cases, length `4` accounts for only about `24.7%..32.9%` of large-watch visits, length `5..9` is usually the biggest bucket at `34.3%..45.3%`, and length `10+` still accounts for `25.2%..40.4%`. So a narrow 4-literal fast path is unlikely to cover most of the remaining learnt-large work by itself. The newest large-scan unroll reject adds the same warning at the loop level: even though average large-clause probe depth is only about `2.3..3.4`, manually spelling out the first two replacement candidates still regressed the five-case serial hotspot slice in both orders (`28.3822s -> 29.6272s` forward, `30.2405s -> 31.4678s` reverse) while keeping identical conflict counts on every case. The newest success-depth profiler upgrade tightens that boundary further: successful large relocations really are head-heavy, with the first two probe steps covering `75.59%` on `large/test_6.cnf`, `74.81%` on `special/hard.cnf`, `69.75%` on `medium/test_4.cnf`, and only `62.76%` on `large/test_8.cnf`, but the manual scan-head unroll still lost cleanly. So low average probe depth and even head-heavy success distribution are not enough, by themselves, to justify another Python scan-head micro-unroll. The already-known-unassigned assignment handoff is now cheaper, so future propagation work should target the watched-clause traversal that happens before the unit/conflict handoff, especially on original ternary clauses, not revisit the exact same inlined-assignment idea. If large-clause scan work is revisited at all, compare it against `large/test_8.cnf`, where scan failures are deeper (`4.85` probes on average), successful relocations have a fatter `5+` tail (`20.30%`), and learnt clauses stay longer. Do not eagerly relocate ternary watches just because the other watched literal is already `TRUE`: that branch regressed the serial five-case slice from `28.4768s` to `36.4731s`, including `large/test_6.cnf 12.6142s -> 17.9193s` and `large/test_8.cnf 3.5857s -> 6.4813s`. Also do not change watch-list payloads just to encode the watched slot: the temporary `SideWatchSolver` lost `27.2088s -> 27.7040s` on the same five-case slice. And do not just defer watched-slot normalization until relocation happens: the temporary `LazyNormalizeSolver` still regressed `27.8534s -> 28.3930s`, and the new profiler counters explain why. On the hotspot cases, only about `20%..27%` of watched-slot normalizations are immediately followed by a satisfied skip, so most normalizations still feed real relocation/unit/conflict work. Ternary clauses dominate those normalization counts too, so future propagation work should aim at removing larger ternary traversal chunks rather than just avoiding the swap. The newest relocation-outcome split tightens that same boundary again: only about `13.5%..17.2%` of ternary relocations go to already-`TRUE` candidates, while roughly `82.8%..86.5%` go to `UNASSIGNED` candidates. The newest other-watch-state split sharpens it even further: only about `1.5%..2.0%` of ternary relocations happen with the other watched literal already `FALSE`, so rescue-style relocation branches are an even smaller minority than satisfied-candidate ones. Combined, that means the dominant path is ordinary ternary relocation with `candidate=UNASSIGNED` and `other=UNASSIGNED`, not a narrowly satisfied or rescue branch. The newest satisfied-skip split closes the remaining “maybe the early exit dominates” loophole too: only about `19.4%..26.4%` of problem-ternary trigger events end in an immediate satisfied skip, so the original-ternary hotspot still mostly flows deeper into relocation, unit propagation, or conflict handling. The newest ternary family-outcome split tightens the same boundary once more: after excluding satisfied skips, problem-ternary conflicts are still under `1%` on the main hotspot cases, while relocation and unit propagation split the remaining work roughly `51%..61%` versus `38%..48%`. The newest ternary normalized-outcome split sharpens that further: only about `31.2%..33.7%` of problem-ternary relocations and `27.1%..29.8%` of problem-ternary units happen after watched-slot normalization, while problem-ternary conflicts are more normalization-heavy at `47.5%..48.0%` but are still under `1%` of non-satisfied original-ternary traffic. So watch-order or swap-avoidance ideas would only touch a minority of the dominant relocation/unit workload. The newest trail-append split adds the list-churn view of that same result: propagation trail appends are about `3.6x..4.1x` larger than `analyze()` learnt-literal appends, are themselves `91.5%..95.9%` ternary-unit work, and are still smaller than raw watch relocations by about `1.31x..2.03x`. The newest watcher-pop split closes the remaining list-mutation loophole on the `pop()` side: watcher-list pops now account for `98.43%..98.92%` of the explicitly counted pop traffic on the four main hotspot cases, while trail-limit pops are noise by comparison. The newest pop-return removal reject tightens that boundary one more step: even after isolating watcher removals as the pop hotspot, replacing the current swap-pop removal with a pop-return variant still regressed the two-order hotspot average (`27.9582s -> 28.1354s`) while preserving exact conflict counts on every case. So future watcher-churn work still needs a larger structural reduction than shaving one indexed read off the current swap-pop pattern. The newest watcher-pop cause split sharpens the target inside that remaining churn: deleted-watch cleanup is only about `1.7%..2.5%` of watcher pops on the main hotspots, while original problem-ternary relocations alone account for `56.4%..73.9%` and learnt large-clause relocations account for another `21.4%..38.3%`; problem large-clause relocation pops are `0` on these pure-3-SAT instances. So if watcher churn is attacked again, target original-ternary relocation first, with learnt-large relocation as the secondary bucket. Likewise, do not revisit a simple per-visit `len(lits)` local cache inside `propagate()`: the temporary `LenCachedSolver` regressed the same serial five-case slice from `27.5712s` to `29.1215s`, including clear losses on `special/hard.cnf`, `medium/test_4.cnf`, and `large/test_8.cnf`. The newest branch-order reject adds one more low-value path to avoid: even though units massively outnumber conflicts after failed ternary and large replacement scans, a unit-first tail rewrite still failed the stricter same-day repo-vs-baseline exact-CLI hotspot A/B (`25.9713s -> 25.5458s` forward, `25.5206s -> 26.3185s` reverse) while preserving exact search counts on the main profiled cases. And the newest clause-order reject extends that warning upstream: raw signed phase-bias watch ordering for ternary problem clauses improved the serial hotspot slice and crushed `large/test_8.cnf`, but still lost the actual exact-CLI hotspot gate by making `large/test_6.cnf` and `large/test_10.cnf` too much worse. The newest in-loop family fast-path reject tightens the same boundary again: simply hoisting non-learnt ternary clauses ahead of the general watcher logic still regressed the serial five-case slice (`29.3791s -> 30.4336` average), helping `large/test_6.cnf` but hurting `special/hard.cnf`, `medium/test_4.cnf`, `large/test_10.cnf`, and `large/test_8.cnf`. The newest blocker-cache reject pushes that warning even further: a per-clause “known satisfied literal” cache for original ternary clauses catastrophically regressed the same slice (`29.9739s -> 68.0094` average) and blew `large/test_8.cnf` up from about `3.5s` to `23..25s`. The newest large-clause reject adds the corresponding warning on the learnt side: a rotating replacement-scan hint per large clause also regressed badly (`30.9232s -> 44.1039` average), hurting every real bottleneck except `large/test_10.cnf`. The newest fixed-trigger reject adds the strongest warning yet against blunt original-ternary rewrites: replacing watched propagation for problem 3-clauses with a static per-literal trigger network nearly doubled the forward hotspot slice (`29.8075s -> 57.8977s`) and blew `large/test_8.cnf` up from `3.6618s` to `28.3463s`, with conflicts `16,430 -> 113,573`. The newest clause-concentration profiler upgrade closes one more tempting door: this work is not concentrated on a few pathological problem clauses. On the main hotspot cases, clause coverage is effectively `100%`, and the hottest single problem ternary clause still accounts for only about `0.16%..0.26%` of problem-ternary visits. The newest trigger-literal concentration upgrade closes the analogous watch-list door too: trigger-literal coverage is also essentially total (`99.8%..100%`), and the hottest single trigger literal still accounts for only about `0.41%..0.67%` of problem-ternary visits. So future propagation work still needs a clearer structural win than swapping rare-tail branch order, statically ordering ternary clauses by raw phase bias alone, merely hoisting the dominant ternary family ahead of the general loop, adding blocker-style / scan-hint side caches in pure Python, visiting every false-literal incidence through a fixed ternary trigger network, targeting only the already-`TRUE` candidate relocation minority, targeting only the tiny `other=FALSE` rescue minority, targeting mainly the immediate satisfied-skip exit, targeting mainly the rare ternary-conflict tail, targeting mainly watched-slot normalization itself, trying another clause-specific or literal-specific cache/order heuristic that assumes a tiny subset of the workload dominates the cost, assuming a 4-literal-only fast path will cover most of the remaining learnt-large watch traffic, or aiming list-churn work at trail-limit maintenance instead of watcher churn.
Additional watch-order boundary: the newest ternary normalized-outcome split says only about `31.2%..33.7%` of problem-ternary relocations and `27.1%..29.8%` of problem-ternary units happen after watched-slot normalization, while even the more normalization-heavy problem-ternary conflict path is only about `47.5%..48.0%` normalized on a branch that is already under `1%` of non-satisfied problem-ternary traffic. So future watch-order, watched-slot, or swap-avoidance ideas would only touch a minority of the dominant relocation/unit workload.
6. Treat binary-path and binary-reason micro-optimizations as low priority for now. The upgraded profiler shows only `11,953` binary checks on `large/test_6.cnf`, `14,395` on `special/hard.cnf`, and `1,796` on `medium/test_4.cnf`, which is tiny relative to watched-clause work, and the new reason split shows binary conflict-analysis traffic is also negligible (`1,248 / 1,057,846` `analyze()` traversals on `large/test_6.cnf`, `1,418 / 770,039` on `special/hard.cnf`, `165 / 181,784` on `medium/test_4.cnf`, and `0 / 337,536` on `large/test_8.cnf`).
7. Keep using both the validated in-process harness and the exact-CLI mode for future solver-speed changes, and prefer `--repeat 2` whenever the keep/revert decision depends on a sub-5% timing swing. Use `SATSOLVER_DISABLE_PORTFOLIO=1` when you need an apples-to-apples serial baseline. Startup overhead has already been trimmed by lazy-loading `multiprocessing` and replacing the slotted `@dataclass` `Clause` with a hand-written slotted class, so future easy CLI gains will likely be smaller. If a solver-core change only claims about a 1% win, demand same-day exact-CLI confirmation against a current baseline rather than relying on older snapshots: the new binary/ternary `minimize_learnt()` fast path improved the repeat-aware in-process full suite (`26.4508s -> 26.2118s`) but the repeat-aware exact-CLI reruns landed at `28.8855s` and `28.9681s`, which did not beat the older `28.7122s` exact-CLI repeat-aware result. This newest keep sharpens that rule further: direct baseline-vs-experiment A/B on the actual hotspot slice now matters more than archived repeat-aware snapshots for tiny propagation changes. The watched-clause learnt-only deleted check looked ambiguous against older full-suite artifacts, but same-day A/B stayed positive on both the serial hotspot slice (`31.0317s -> 29.9646s`) and the exact CLI hotspot slice (`25.9677s -> 25.7970s`) while full-suite repeat2 runs wandered enough that they were not trustworthy by themselves. So if module-mode and exact-CLI full-suite reruns disagree or drift around, use same-search hotspot A/B and exact-CLI hotspot A/B to break the tie, and log the mixed outcome explicitly instead of over-trusting one noisy full-suite rerun. Also be careful with `cProfile`-tempting branch-picker work: `pick_branch_literal()` still costs real time, but the cheap best-variable hint cache preserved identical decisions/conflicts on the hotspot cases and still regressed, the newest local-alias cleanup branch also looked mildly positive on both hotspot slices (`26.6864s -> 26.3141s` serial, `23.6990s -> 23.4572s` exact CLI) before losing the repeat-aware exact-CLI 59-case suite (`27.1974s -> 27.3229s`), and the newest active-unassigned-prefix scratch branch failed even harder, regressing the very first dense gate on `large/test_6.cnf` from `15.0435s` to `28.8075s` while blowing decisions up `72,886 -> 134,042` and conflicts `59,201 -> 108,066`. The new branch-frontier profiler counters say that on the main hotspots the branch frontier is still wide (`avg_branch_unassigned` about `161.67..247.72`), but the winning best-activity variable is almost always unique (`9`, `13`, `15`, and `15` multiway ties total across the four main cases) and zero-activity branch choices are absent. So future branching work should not target tie handling or zero-activity fallback first; if it is revisited at all, it needs a read-only scan reduction or another stronger invariance story than mutable active-set maintenance. The newest `solve()` helper cleanup reject extends the same warning to outer-loop pruning: even combining gated `reduce_database()` calls with inlined decay divisions only moved the exact-CLI hotspot average from `23.3898s` to `23.3875s`, which is effectively noise. The newest learnt-path reject adds the same warning to conflict-path object cleanup: even removing the extra learnt-clause list copy in `add_learnt_clause()` split by order and regressed slightly on average (`29.2186s -> 29.5014s`) on the serial hotspot slice. So future branching, `solve()`-level helper work, or learnt-storage helper work should demand a stronger structural story than local memoization, loop-local caching, call-boundary cleanup, explicit mutable active-set maintenance, or “one less list copy” object-lifecycle trims alone. If portfolio work is revisited, demand a materially stronger alternate worker than the current options: after the latest resweep, `phase_bias` still helps basically only `large/test_8.cnf`, `all_true` is almost indistinguishable from default (`27.3121s` vs `27.3625s` on the serial five-case slice), and `all_false` is still unstable. The newest `maybe.md` revisit adds one more boundary: a raw-`fork` rewrite of the existing two-worker portfolio made `large/test_8.cnf` faster (`0.2892s -> 0.2626s` on repeated exact-CLI runs) but still lost the same-day repeat-aware exact-CLI 59-case suite (`27.1974s` baseline vs `27.8860s` experimental), so portfolio plumbing churn alone is not enough. Compare any new portfolio idea against the current reduced-startup exact-CLI baseline rather than older snapshots, and require a broad suite-level win rather than a single-case microbenchmark.
8. Do not revisit simple root failed-literal probing on the dense hotspots without a stronger candidate selector: checking both polarities of the top `24` activity-ranked variables found `0` failed literals on `large/test_6.cnf`, `special/hard.cnf`, `medium/test_4.cnf`, `large/test_10.cnf`, and `large/test_8.cnf`.
9. Treat fixed-polarity phase seeding as unproven and unstable for now. The latest dense five-case resweep still favored the default worker at `29.0347s`, with `phase_bias` losing at `30.7907s`, `all_true` at `29.8937s`, and `all_false` at `43.1369s` despite crushing `large/test_8.cnf`. Any future phase-policy work needs a better classifier than clause density or current literal-bias statistics alone.
10. The current local portfolio gate still matches only `large/test_8.cnf`. If that logic is revisited, benchmark any simplification or replacement against the current path over multiple exact-CLI repeats, because serial bias seeding and the two-process portfolio are already very close on single-run timings. The newest dense-worker sweep closes off the most obvious extension of that idea too: the high-density pure-3-SAT cases (`large/test_6.cnf`, `special/hard.cnf`, `medium/test_4.cnf`, `large/test_10.cnf`) do separate cleanly from `large/test_8.cnf` by density, but simple alternate workers still lost there. On the same five-case serial sweep, the retained default worker finished at `28.9875s`, while `restart_base=32` took `36.5529s`, `restart_base=128` `46.3689s`, `var_decay=0.94` `35.6389s`, `var_decay=0.96` `31.1810s`, and `all_true` `31.5186s`. The variants that crush `large/test_8.cnf` are still not dense-UNSAT specialists, so any future portfolio expansion needs a materially different alternate search identity than local restart/decay/phase tweaks.
11. Keep the new root pure-literal presolve narrow unless a stronger exact-CLI signal appears. The ungated version was a clear trap: even one root pure assignment could perturb the search path badly enough to blow up `large/test_6.cnf` and `large/test_8.cnf`, taking a same-day exact-CLI mixed slice from `22.2586s` to `25.1953s`. The retained gate at `>= 2` root assignments recovered that and improved the same-day mixed exact-CLI slice to `22.0635s`, while also improving the repeat-aware in-process full suite from `25.2549s` to `25.1541s`. But two repeat-aware exact-CLI full-suite reruns still landed at `27.7868s` and `27.6136s`, so the historical `27.1974s` exact-CLI best remains unbeaten. Future presolve work should treat low-yield root simplifications as search-path perturbations first, benchmark them against same-day exact-CLI slices before broadening them, and prefer stronger gating/classification over always-on preprocessing.
12. Do not assume a larger pure-assignment threshold is automatically safer, and do not assume removed-clause ratio by itself fixes that problem. A follow-up sweep found that raising the gate to `>= 5` assignments looked clearly better on both the exact-CLI pure-hit slice (`0.4591s -> 0.4217s`) and the same-day exact-CLI mixed slice with the main hotspots (`22.5906s -> 22.1814s`), yet still lost the repeat-aware exact-CLI 59-case suite at `27.7944s`. A newer temporary gate that piggybacked on the same fixpoint and required both `>= 2` assignments and removed-clause ratio `>= 0.08` also looked good on the pure-hit slice (`0.5957s -> 0.5573s` against its local `0.00` baseline) and the same-day mixed slice (`26.0581s -> 25.4692s`), and even stayed positive on the stricter forward/reverse mixed A/B. But it still failed the real repeat-aware exact-CLI 59-case suite badly at `31.4387s`. So future root pure-literal tuning needs a classifier stronger than assignment count or removed-clause ratio alone, and slice-local exact-CLI wins are still not enough by themselves to justify changing the retained gate.
13. Do not add another whole-formula front-loaded purity pass just to decide whether to run the current root pure-literal presolve. A follow-up experiment found a seemingly perfect corpus predictor: every currently useful case with total root pure assignments `>= 2` already has at least two first-round pure literals, and all the one-pure cases stay at one. But turning that into an extra first-round screening pass still regressed the repeat-aware exact-CLI 59-case suite badly (`29.3678s`), even though the same-day mixed slice improved slightly (`23.4612s -> 23.3365s`). So future root-pure gating should avoid extra full normalization/polarity scans unless they remove much larger downstream work than this did.
14. Treat manual ternary-clause local unpack/write-back inside `propagate()` as another dead end unless a much stronger structural change comes with it. A scratch branch that manually unpacked ternary clauses into locals and wrote the watched order back explicitly still regressed the five-case hotspot slice in both orders (`29.6744s -> 31.0203s` forward, `28.7830s -> 30.9007s` reverse) while keeping identical conflict counts on every case, so shaving a few `lits[...]` reads by itself is not enough.
15. Treat one-pass normalized-clause reuse through root pure discovery and clause ingest as another weak startup cleanup, not a proven win. The scratch branch preserved exact hotspot conflict counts and was nearly tied on the serial five-case slice, but it still lost the real exact-CLI full suite slightly (`29.7802s -> 29.8429s`) while staying `59/59` correct. So future preprocessing/startup cleanup should demand a stronger suite-level signal than “remove one duplicate normalization pass.”
16. Treat fixed-order problem ternary clauses plus watched-position side arrays as another dead end unless a stronger simplification comes with them. An out-of-tree `OrientationSolver` kept exact conflict counts on every hotspot case and still regressed badly in both orders (`35.4075s -> 38.7377s` forward, `33.8519s -> 37.5611s` reverse), so replacing `clause.lits` mutation with extra watched-position state is not enough by itself.
17. Do not assume active problem-ternary watcher batches are already homogeneous, but also do not treat physical list separation by itself as the missing win. The profiler split still says batches containing problem ternary clauses are mixed `47.21%..70.78%` of the time on the main hotspots, with `0.96..2.36` non-problem co-resident watchers per batch on average, mostly learnt-large traffic. But the bounded scratch `SplitWatchSolver` that actually moved original problem-ternary clauses into dedicated watch lists still changed the search path badly enough to regress hard on `large/test_6.cnf` (`11.9614s` / `59,201` conflicts -> `16.9394s` / `81,161`) and `large/test_8.cnf` (`3.3173s` / `16,430` -> `9.2863s` / `44,287`), even though it helped `special/hard.cnf`. So future split-list work needs a much stronger semantics-preservation story than “the batches are mixed, therefore separate the lists,” and it still must stay clearly distinct from the already-rejected branch-hoist and clause-local-side-state directions.
18. Treat `satsolver_blaze.py` as a stale alternate-worker lead on the current hotspot family. A direct module-mode comparison now has the retained solver clearly ahead in both orders (`29.8179s` vs `35.5141s` forward, `35.8424s` vs `43.0727s` reverse), with blaze only stealing one forward `large/test_10.cnf` run. Future portfolio work needs a more materially different and current search identity than simply reviving the older sibling solver.
19. Use `tools/hotspot_compare.py` for future forward/reverse hotspot A/B checks before spending full-suite benchmark time on small solver changes. The tool already supports module-vs-module, module-vs-CLI, and CLI-vs-CLI comparisons with repeat-aware representative times and checker-backed validation, so it should replace the recent ad hoc shell snippets.
20. Treat `satsolver_fast.py` as a meaningful exact-CLI alternate candidate, not dead code and not yet the new mainline. The lean wrapper that removes root pure-literal presolve while keeping the current CDCL core, presolvers, and portfolio path beat the same-day exact-CLI 59-case single run `29.3963s -> 28.4781s` and landed `27.3829s` repeat-aware in `out_fast_cli_extended.txt`, both `59/59` correct. But it still did not beat the historical best exact-CLI snapshot for `satsolver.py` (`27.1974s`). So future exact-submission work should use `satsolver_fast.py` as the current alternate baseline, and any attempt to merge that win back into `satsolver.py` should target a smarter root-pure policy rather than just deleting the presolve blindly.
21. Do not remove the post-solve `model_satisfies(...)` self-check from `satsolver_fast.py` unless a future benchmark corpus makes that path materially larger. On the current benchmark set, the entire SAT-only check totals just `0.005311s` across all `28` SAT formulas, with the worst single case only `0.000713s`. A repeat-aware exact-CLI rerun of the no-self-check variant still landed at `27.6785s`, worse than the retained `satsolver_fast.py` snapshot `27.3829s`. So CLI-only safety removals of this size are not worth keeping.
22. Do not assume parser-integrated first-round-pure gating is enough to rescue root-pure preprocessing on `satsolver_fast.py`. Even after confirming that all current root-pure hit cases already have `first_round_pure_count >= 2`, a branch that collected those counts during DIMACS parsing and only then ran iterative root-pure preprocessing was still a near-tie at best: it regressed slightly on the exact-CLI root-hit slice (`0.4382s -> 0.4407s`) and was only effectively tied on the mixed exact-CLI slice (`24.6599s -> 24.6480s`). So future exact-CLI root-pure work still needs a materially stronger classifier or a broader win than this parser-gate branch showed.
23. Do not assume a standalone no-root-pure copy of the full solver is better than the current wrapper-style `satsolver_fast.py`. A branch that replaced the wrapper with a copied `satsolver.py` minus root-pure presolve regressed badly on the exact-CLI root-hit SAT slice (`0.4336s -> 0.4996s`) and was only slightly positive on the mixed nine-case hotspot slice (`24.4869s -> 24.4441s`). So future alternate exact-CLI files need to be materially smaller or more structurally different than a duplicated main solver, not just “the same code without root-pure.”
24. Do not assume a smaller wrapper around the same no-root-pure alternate core is enough to beat the retained `satsolver_fast.py`. A scratch min-wrapper that trimmed the current alternate file down further still lost the exact-CLI root-hit SAT slice on the two-order average (`0.4159s -> 0.4511s`) and then lost the repeat-aware exact-CLI 59-case suite overall (`27.3829s -> 27.5180s`), even though it improved the mixed nine-case hotspot slice (`24.8028s -> 24.5644s`). So future alternate exact-CLI file work needs a more meaningful semantic or code-size win than wrapper reshaping around the same no-root-pure solver core. The same cycle also measured full-corpus structural presolver cost directly: pigeonhole plus XOR checks totaled only about `0.0643s` across all 59 benchmark cases, so dropping those presolvers is unlikely to be the next useful exact-CLI lever either.
25. Do not assume a lazy-import alternate file with duplicated parser/output/structural presolvers is enough to beat the retained `satsolver_fast.py`. A scratch `scratch_satsolver_fast_lazy.py` did improve the six-case structural-hit exact-CLI slice (`0.1811s -> 0.1780s`), stayed slightly positive on the mixed nine-case hotspot slice (`24.7933s -> 24.7768s`), and even beat the retained alternate on a same-day exact-CLI single-run full suite (`27.1851s -> 26.9872s`). But it still lost the repeat-aware exact-CLI 59-case gate at `27.5546s`, worse than the retained `27.3829s`. So future alternate exact-CLI file work should not trust startup-sensitive slice wins or a one-off full-suite win by themselves; it still needs a stronger repeat-aware suite-level signal than “lazy-load the main solver behind duplicated structural presolvers.”
26. Do not drop the structural pigeonhole/XOR presolvers from future exact-CLI alternate files just because their aggregate corpus cost is small. A scratch lazy-core alternate that kept only local parse/output work and lazy-imported the solver core without those presolvers regressed the SAT-heavy root-hit slice (`0.4081s -> 0.4182s`) and catastrophically regressed the mixed exact-CLI hotspot slice (`24.3616s -> 32.2099s`) because `special/tseitin.cnf` alone blew up from about `0.03s` to about `7.43..7.46s`. So future alternate-file work should treat those structural shortcuts as required exact-CLI fast exits, not optional startup overhead.
27. Treat exact-CLI DIMACS parse/write overhead as a real but small lever on `satsolver_fast.py`. Replacing the inherited text parser/writer with a byte-level local `parse_dimacs[_file]()` plus byte-output path improved the same-day SAT-heavy root-hit exact-CLI slice (`0.4431s -> 0.4319s`) and improved the same-day repeat-aware exact-CLI 59-case comparison against the retained wrapper from `27.6459s` to `27.4523s`, both `59/59` correct. But the mixed nine-case hotspot slice was effectively tied (`24.6944s -> 24.6945s`), the refreshed merged artifact still landed at `27.5074s`, and the historical best retained `satsolver_fast.py` snapshot `27.3829s` remains unbeaten. So future alternate exact-CLI file work can revisit parser/read/write overhead, but it should treat gains here as sub-1% and noisy, require same-day repeat-aware baseline-vs-candidate measurements, and still keep the structural pigeonhole/XOR presolvers intact.
28. Do not assume the same byte-level DIMACS parse/write path that helped `satsolver_fast.py` will also help the main submission file `satsolver.py`. A main-solver branch using byte-level `parse_dimacs_file()` and byte-output `write_result()` improved the exact-CLI SAT-heavy root-hit slice (`0.4960s -> 0.4718s`) and was nearly tied on the mixed nine-case hotspot slice (`24.6647s -> 24.6994s`), but it still regressed the same-day repeat-aware exact-CLI 59-case suite badly (`27.6153s -> 29.3807s`), both `59/59` correct. So future parser/read/write work should stay on alternate exact-CLI wrappers unless a main-solver branch can clear the same-day repeat-aware full-suite gate; slice-local wins are not enough.
29. Treat lazy structural wrappers around `satsolver_fast.py` as another dead end unless the future design is materially different. A second-generation scratch wrapper with local byte-level parse/output, local structural pigeonhole/XOR fast exits, and lazy import of `satsolver_fast` still regressed the structural-hit slice (`0.1857s -> 0.1924s`), the SAT-heavy root-hit slice (`0.4189s -> 0.4208s`), and the mixed nine-case hotspot slice (`24.3005s -> 24.6247s`). So the “do local structural checks, then import the current alternate solver anyway” idea does not appear to recover the old lazy-import story after the bytes-parser keep.
30. Treat direct streamed-file DIMACS parsing in `satsolver_fast.py` as another dead end unless a future parser design removes materially more work than the current byte parser does. A scratch candidate that replaced the retained `read() + splitlines()` byte parser with a streaming binary-line loop regressed the first exact-CLI gate from `0.3701s` to `0.3931s` on the SAT-heavy root-hit slice (`0.3721s -> 0.3778s` forward, `0.3680s -> 0.4083s` reverse). So parse-path work on the alternate exact-CLI file now looks especially noise-sensitive and should demand a stronger same-day signal before earning broader benchmarking.
31. Treat “replace the narrow portfolio gate with one serial phase-biased worker” as another dead end. A branch that changed `satsolver_fast.py` so the current portfolio gate called `solve_cnf_serial(..., seed_phase_bias=True)` instead of the two-worker portfolio did produce a real exact-CLI `large/test_8.cnf` win (`0.2922s -> 0.2207s` two-order average), but it still lost the mixed nine-case exact-CLI hotspot slice overall (`24.3702s -> 24.5513s`) because the reverse-order regressions outweighed the forward win. So future portfolio work still needs a broader suite-level story than just avoiding `fork()` on the one gated SAT-like family.
32. Treat broader learnt-database trimming aggressiveness as another dead end unless a future idea brings a materially better classifier than “same ranking, keep a smaller fraction.” A branch that changed `reduce_database()` to keep only the top `40%` of ranked candidates instead of the top `50%` blew up the mixed nine-case exact-CLI hotspot slice from `24.6309s` to `40.4076s`, with `large/test_8.cnf` alone jumping from about `0.29s` to about `12.18s`. So future learnt-large watcher work should not try to solve the problem by simply trimming the ranked candidate pool harder across the board.
33. Treat parser-path work in `satsolver_fast.py` as another exhausted lane unless the future design changes much more than how the bytes are tokenized. A scratch manual byte-scanner parser regressed the first exact-CLI gate from `0.4605s` to `0.4908s` on the SAT-heavy root-hit slice, after the earlier streamed-line parser already lost `0.3701s -> 0.3931s`. So the retained bytes parser keep now looks like the end of the easy exact-CLI parser wins, not the start of a larger parser-optimization ladder.
34. Treat “same ranking, same keep fraction, reduce sooner” as another learnt-database dead end unless a future branch brings a materially better classifier than a smaller `next_reduce` multiplier. A branch that only changed the post-reduction schedule from `int(len(self.learnt_ids) * 1.5) + 64` to `int(len(self.learnt_ids) * 1.25) + 64` regressed the mixed nine-case exact-CLI hotspot slice from `24.4791s` to `30.9079s`, with `large/test_8.cnf` alone jumping from about `0.29s` to about `2.32s` and the dense UNSAT cases also getting worse. So future learnt-large work should not assume that “reduce earlier” is a safer version of aggressive trimming.
35. Treat “same keep fraction, but prefer shorter learnt clauses before activity within the same LBD bucket” as another learnt-database dead end unless a future branch brings a much stronger classifier. A branch that changed the reduction sort from `(lbd, -activity, len)` to `(lbd, len, -activity)` regressed the mixed nine-case exact-CLI hotspot slice from `24.9575s` to `30.6752s`, with `large/test_6.cnf` jumping from about `12.50s` to about `16.86s` on the two-order average and `large/test_8.cnf` tripling from about `0.28s` to about `0.91s`. So future learnt-large work should not assume that shorter-first tie-breaking is a safe way to attack watcher churn.
36. Treat narrow exact-CLI hybrids between `satsolver_fast.py` and `satsolver.py` as another dead end unless a future selector has a much stronger classifier than `num_vars`, density, and all-3-SAT shape. A scratch wrapper that kept `satsolver_fast.py` as the default path but routed formulas with `num_vars >= 250`, density `> 5.0`, and all clauses length `3` back to `satsolver.py` regressed the mixed nine-case exact-CLI hotspot slice from `24.6659s` to `25.0305s`. The intended `large/test_6.cnf` rescue only appeared in one order, while `special/hard.cnf`, `large/test_10.cnf`, and small startup-sensitive cases gave the gain back. So future hybrid exact-CLI work should not assume that a one-band dense-3-SAT selector is enough.
37. Treat learnt-clause activity bumping as still essential to the current solver unless a future branch replaces its ranking signal with something materially better. A branch that removed `self.bump_clause_activity(current_clause_id)` from `analyze()` regressed the mixed nine-case exact-CLI hotspot slice from `25.5245s` to `34.0354s`, with `large/test_6.cnf`, `special/hard.cnf`, and `large/test_8.cnf` all getting substantially worse. So future learnt-database work should not assume that clause activity is merely low-value bookkeeping because its direct runtime cost is modest.
38. Treat size-normalized learnt-clause activity as another dead end unless a future classifier changes more than the sort key. A branch that kept the same top-half retention and reduction schedule but ranked candidates by `(lbd, -(activity / len), len, -activity)` still regressed the mixed nine-case exact-CLI hotspot slice from `25.3946s` to `30.0746s`, with the main damage again landing on `large/test_6.cnf` and `large/test_8.cnf`. So future learnt-database work should not assume that normalizing activity by clause length is a safe middle ground between the retained ranking and the already-rejected shorter-first tie-break.
39. Treat the promoted helper split as the new submission baseline. `satsolver.py` now uses the byte-parser/no-root-pure exact-CLI wrapper over `satsolver_core.py`, while still exporting the old core API (`Solver`, `Clause`, `solve_cnf_serial()`, root-pure helpers, portfolio helpers) for tests and tooling. That promotion beat the previous mainline on the same-day root-hit exact-CLI slice (`0.5026s -> 0.4044s`), the mixed nine-case exact-CLI hotspot slice (`24.5923s -> 24.1644s`), and the same-day repeat-aware exact-CLI 59-case suite (`30.3078s -> 29.5746s`), all `59/59` correct. Future exact-CLI work should benchmark against this promoted wrapper, not the old monolithic main file.
40. Do not assume the sibling `satsolver_fast.py` is obsolete just because the wrapper path was promoted. On the same day, the promoted `satsolver.py` beat `satsolver_fast.py` on the root-hit slice (`0.4516s -> 0.4391s`) and on the mixed nine-case exact-CLI hotspot slice (`25.2656s -> 24.5747s`), but `satsolver_fast.py` still landed a slightly better broad repeat-aware exact-CLI rerun (`29.1527s` vs promoted `29.5746s`). So future exact-CLI decisions should use same-day hotspot A/B plus same-day repeat-aware full-suite A/B together; neither sibling should be assumed dominant from one artifact alone.
41. Treat runtime-only import cleanup as a real but narrow exact-CLI lever on the promoted wrapper baseline. Removing runtime `typing` imports from `satsolver.py`, `satsolver_fast.py`, and `satsolver_core.py` improved the 12-case SAT-heavy root-hit slice (`0.4338s -> 0.3878s`), the mixed nine-case exact-CLI hotspot slice (`24.5782s -> 24.4033s`), and the same-day repeat-aware exact-CLI 59-case suite (`28.3088s -> 27.9510s`), all still correct. But the broad gain is only about `1.26%`, so future startup-path work should target similarly heavy imports or wrapper/module overhead and still require same-day repeat-aware 59-case confirmation; import-time traces and startup-sensitive slices alone are not enough.
42. Do not assume the remaining top-level wrapper/core imports are another free exact-CLI win after the `typing` cleanup. A follow-on branch that removed the remaining top-level `os`/`sys` imports from `satsolver_core.py` and moved them into the few functions that actually use them, while also moving `sys` into `main()` in `satsolver.py` and `satsolver_fast.py`, did improve the 12-case SAT-heavy root-hit slice (`0.4387s -> 0.4052s`) but still regressed the mixed nine-case exact-CLI hotspot slice from `23.9617s` to `24.2357s`. So future startup-path work should treat the kept `typing` cleanup as the useful stopping point for this lane unless a new idea can show a broader solver-path win than import trimming alone.
43. Treat “drop the wrapper's unused export ballast” as another exact-CLI startup dead end unless a future alternate-file idea changes substantially more than alias surface area. A scratch `scratch_satsolver_leanfast.py` wrapper that kept the same byte parser, structural presolvers, and solve path but omitted most of the broad exported alias surface still lost the 12-case SAT-heavy root-hit exact-CLI gate on the two-order average (`0.3571s -> 0.3623s`). Reintroducing only the hot internal aliases also still lost (`0.3474s -> 0.3530s`) and made the import-time smoke check worse. So future alternate-file startup work should not assume that reducing exported wrapper names is a meaningful lever by itself.
44. Treat tokenized locked-clause marks in `reduce_database()` as another learnt-database micro-optimization dead end unless a future branch changes more than one small allocation site. A branch that replaced the per-reduction Python `set` of locked clause IDs with reusable `clause_marks` / `clause_mark_token` state still regressed the mixed nine-case exact-CLI hotspot slice from `24.9111s` to `25.8251s`. The forward order was slightly positive (`25.1938s -> 24.9983s`), but the reverse order lost badly (`24.6283s -> 26.6520s`), with the biggest damage on `large/test_6.cnf`. So future `reduce_database()` bookkeeping work should not assume that replacing a small `set` allocation with mark arrays is automatically an end-to-end win.
45. Treat parse-time structural-summary collection as another exact-CLI startup dead end unless a future parser design makes the summary work materially cheaper than the current standalone scans. A scratch `scratch_satsolver_statsfast.py` wrapper that collected pigeonhole, XOR, and portfolio summaries incrementally during parsing regressed the 12-case SAT-heavy exact-CLI root-hit slice from `0.3818s` to `0.4267s` on the two-order average. So future alternate exact-CLI work should not assume that folding structural presolve checks into parsing is a free win; a heavier parser can easily cost more than the rescans it removes.
46. Treat lazy export forwarding in `satsolver.py` as another exact-CLI startup dead end unless a future wrapper design removes more than top-level alias binding. A branch that replaced the main wrapper's broad alias block with module `__getattr__` forwarding and patch-friendly `globals().get(..., base....)` fallbacks did barely improve the 12-case SAT-heavy root-hit slice (`0.3592s -> 0.3524s`) but still regressed the mixed nine-case exact-CLI hotspot slice from `24.1326s` to `24.4102s`, with the biggest reverse-order damage on `special/hard.cnf`. So future `satsolver.py` startup work should not assume that delaying export resolution is enough to produce a net submission-path win.
47. Treat direct SAT-output byte emission as another exact-CLI startup dead end unless a future wrapper design removes substantially more work than `format_model(...).encode("ascii")` alone. A scratch alternate that changed only `write_result()` in `satsolver_fast.py`-style code to build the SAT model line directly with a `bytearray` still regressed the 12-case SAT-heavy exact-CLI root-hit slice from `0.3717s` to `0.3843s` on the two-order average. Combined with the earlier `large/test_8.cnf` profile, which showed output formatting to be tiny next to parse/read and portfolio overhead, this says future submission-path work should stay focused on broader parse/import/solve-path reductions rather than isolated SAT line formatting.
48. Treat tiny immutable clause-shape metadata as a still-live propagation lane, but keep the boundary tight. A core branch that added `Clause.ternary` and used it instead of `len(lits) == 3` in `propagate()` improved the mixed nine-case exact-CLI hotspot slice from `25.9133s` to `25.0078s` and improved the same-day repeat-aware exact-CLI 59-case baseline-vs-candidate comparison from `29.1167s` to `28.3019s`, with the refreshed retained artifact landing at `27.8701s`, all `59/59` correct. But the SAT-heavy root-hit slice was slightly worse (`0.3694s -> 0.3722s`), and the earlier `Clause.size` / mutable side-array experiments still lost. So future propagation work can consider similarly cheap immutable family flags, but should not treat this as permission to resurrect broader duplicated clause metadata or mutable watch-position state.
49. Do not assume every reuse of immutable clause-shape metadata is a free win outside `propagate()`. A scratch branch that reused `Clause.ternary` inside `minimize_learnt()` and removed the local `len(reason_lits)` shape test looked excellent on the mixed nine-case exact-CLI hotspot slice (`24.7815s -> 23.3213s`), but it still regressed the 12-case SAT-heavy root-hit slice (`0.3652s -> 0.3889s`) and the repeat-aware exact-CLI 59-case suite (`26.5996s -> 27.9836s`), with `large/test_6.cnf` worsening from `11.5792s` to `13.0787s`. So future immutable metadata work should keep the same full-suite caution as other small conflict-analysis fast paths; a hotspot win inside `minimize_learnt()` is not enough by itself.
50. Treat hand-unrolled fixed-size reason branches beyond ternary as another likely `minimize_learnt()` dead end unless a future variant shows a much stronger full-slice signal. A scratch exact 4-literal reason fast path regressed the mixed nine-case exact-CLI hotspot slice from `23.9068s` to `24.9093s`, mainly because `special/hard.cnf` worsened from `8.1569s` to `10.1933s` while `large/test_6.cnf` improved only modestly (`11.7708s -> 11.5467s`). So future conflict-analysis specialization should be skeptical of extending the current binary/ternary one-hop unroll pattern to larger fixed reason sizes.
51. Treat schedule-only learnt-database changes as another dead end unless a future branch introduces a materially better classifier than just changing the post-reduction multiplier. A scratch branch that raised the post-reduction `next_reduce` growth factor from `1.5` to `1.75` regressed the mixed nine-case exact-CLI hotspot slice from `23.5526s` to `33.5169s`, with catastrophic damage on `large/test_8.cnf` (`0.2912s -> 6.6520s` forward, `0.2766s -> 6.4384s` reverse) and heavy losses on `large/test_6.cnf` too (`11.5209s -> 15.2828s` forward, `11.3521s -> 15.4253s` reverse). So both “reduce earlier” and “reduce later” are now clearly unsafe without a better learnt-clause retention/reduction classifier.
52. Treat fixed-size watched-clause specialization beyond ternary as another likely propagation dead end unless a future branch shows a materially stronger broad exact-CLI win than the quaternary attempt did. A scratch branch that added `Clause.quaternary` plus a dedicated exact-4-literal watched-clause path in `propagate()` did improve the mixed nine-case exact-CLI hotspot slice (`23.8342s -> 23.6216s`), but it still regressed the 12-case SAT-heavy root-hit slice (`0.3453s -> 0.3528s`) and the repeat-aware exact-CLI 59-case suite (`26.8303s -> 27.4920s`), with the broad damage concentrated on `large/test_6.cnf`, `medium/test_4.cnf`, and `large/test_10.cnf`. So future immutable family-flag work should not assume the next fixed watched size after ternary is worth specializing by default.
53. Treat tiny `append()` / `pop()` lookup cleanup inside `propagate()` as another likely micro-optimization dead end unless a future branch removes more structural work than just rebinding hot list methods. A scratch branch that localized `trail.append` and per-watcher-list `watchers.pop` inside `propagate()` looked positive in forward order (`25.1835s -> 24.5555s`) but still regressed the mixed nine-case exact-CLI hotspot slice on the two-order average (`24.1557s -> 24.2120s`). The persistent losses on `large/test_6.cnf` and `special/hard.cnf` outweighed the unstable forward-order wins on `medium/test_3.cnf` and `medium/test_4.cnf`. So future propagation work should not assume that raw list-method lookup cost can be harvested cleanly without removing more real watcher/trail churn too.
54. Treat explicit CLI-mode/import-mode separation as a still-live wrapper lane, unlike lazy export forwarding or blanket alias trimming. A scratch import-gated `satsolver.py` that only builds the broad alias/test surface when imported, while using `satsolver_core` symbols directly on the CLI path, improved the 12-case SAT-heavy root-hit exact-CLI slice (`0.3905s -> 0.3842s`), the mixed nine-case exact-CLI hotspot slice (`25.2601s -> 23.5388s`), and the same-day repeat-aware exact-CLI baseline-vs-candidate suite (`27.6064s -> 27.5425s`), with the refreshed retained artifact landing at `26.4762s`. Future startup work should prefer this explicit “lean when run as a script, rich when imported” split over lazy export forwarding or alias-surface trimming, but it should still demand same-day repeat-aware full-suite confirmation because the broad A/B margin was modest.
55. Treat trimming the imported standalone CLI surface in `satsolver_core.py` as another startup-path dead end unless a future branch changes something materially different than just skipping dead helper definitions. A scratch pair that made imported `satsolver_core.py` omit its standalone text parser, standalone solve helpers, standalone write path, and `main()` while keeping the real wrapper solve path unchanged still regressed the 12-case SAT-heavy root-hit exact-CLI slice (`0.3984s -> 0.4108s`) and regressed the mixed nine-case exact-CLI hotspot slice much harder on the two-order average (`24.5731s -> 26.5896s`), with reverse-order `large/test_6.cnf` doing most of the damage (`11.8065s -> 15.7812s`). So future startup work should keep preferring the retained main-wrapper import gating over trying to hollow out imported `satsolver_core.py` itself.
56. Treat tiny CLI-local alias sets for genuinely hot `satsolver_core` symbols as a still-live wrapper lane, but only with broad-suite confirmation. A scratch `satsolver.py` that, only in CLI mode, locally bound `Solver`, `model_satisfies`, `has_pigeonhole_core`, `xor_system_unsat`, `format_model`, and `should_use_parallel_portfolio` regressed the 12-case SAT-heavy root-hit exact-CLI slice (`0.3878s -> 0.3997s`) but still improved the mixed nine-case exact-CLI hotspot slice (`23.4102s -> 23.2025`) and the same-day repeat-aware exact-CLI baseline-vs-candidate suite (`27.2824s -> 27.0228`), with the refreshed retained artifact landing at `25.7027`. So future wrapper work can still consider very small CLI-only local aliasing for hot core symbols, but it should keep demanding same-day repeat-aware full-suite confirmation because the startup-heavy SAT slice can move the wrong way even when the broad suite wins.
57. Treat low-density all-3-SAT gating as the only root-pure lane that is currently still alive on the promoted exact-CLI wrapper, and still demand broad-suite confirmation before keeping it. A scratch branch that reintroduced iterative root-pure presolve only when all clauses were length `3` and clause density was at most `3.2` improved the 12-case SAT-heavy root-hit exact-CLI slice (`0.3794s -> 0.3674`), improved the mixed nine-case exact-CLI hotspot slice (`23.5744s -> 23.0621`), and improved the same-day repeat-aware baseline-vs-candidate exact-CLI full suite (`26.1422s -> 25.9837`), all `59/59` correct. But the refreshed retained artifact landed at `26.1805`, still slower than the retained historical best `25.7027`. So future root-pure work should prefer cheap density/shape prefilters over assignment-count-only or removed-clause-ratio gates, while still treating same-day broad-suite A/B as the real arbiter when the refreshed retained artifact is noisy.
58. Treat parse-time `all_ternary` carry-forward as another likely exact-CLI wrapper dead end unless a future branch removes more than the current clause-shape rescan. A scratch `satsolver.py` branch that returned `all_ternary` from the byte parser and threaded it into both the low-density root-pure gate and portfolio gate did improve the 12-case SAT-heavy root-hit exact-CLI slice (`0.4696s -> 0.4536s`), but it still regressed the mixed nine-case exact-CLI hotspot slice on the two-order average (`28.1142s -> 28.6419s`), with `large/test_6.cnf` worsening in both orders (`13.6206s -> 14.0899s` forward, `13.2467s -> 14.9543s` reverse). So future wrapper work should not assume that reusing parse-time clause-shape metadata is enough by itself; it still has to beat the dense mixed gate, not just the startup-heavy slice.
59. Treat `FAST_ROOT_PURE_DENSITY_GATE = 3.2` as a practical lower bound unless a future branch adds a better classifier than density alone. A scratch `satsolver.py` branch that only tightened the gate to `2.7` regressed the 12-case SAT-heavy root-hit exact-CLI slice (`0.5057s -> 0.5126s`) and still regressed the mixed nine-case exact-CLI hotspot slice on the two-order average (`33.6012s -> 34.4853s`), with the main reverse-order damage on `large/test_6.cnf` (`17.6161s -> 19.0170s`) and `special/hard.cnf` (`10.4885s -> 11.6226s`). So future root-pure tuning should not assume that simply tightening the current density cutoff is safer; this lane now looks classifier-limited rather than threshold-limited.
60. Treat the no-root-pure `solve_cnf_fast_serial()` path as the new exact-CLI baseline again. A same-day cycle first showed current `satsolver_fast.py` beating retained `satsolver.py` on the root-hit exact-CLI slice (`0.5025s -> 0.4947s`), the mixed nine-case hotspot slice (`32.7270s -> 32.5444s`), and the repeat-aware exact-CLI 59-case suite (`39.8399s -> 39.1591s`). Promoting that policy back into the import-gated main wrapper then improved the root-hit slice further (`0.4790s -> 0.4603s`), improved the mixed nine-case hotspot slice (`31.9046s -> 31.4055s`), and improved the same-day repeat-aware exact-CLI 59-case suite strongly (`39.8399s -> 36.6465s`), all `59/59` correct. So future presolve work should treat “no fast-path root pure on the submission path” as the current mainline and only revisit root-pure on exact CLI with a materially stronger classifier than density alone plus same-day broad-suite confirmation.
61. Treat dedicated immutable original-ternary branch hoisting as another likely propagation dead end unless a future branch shows a materially stronger broad exact-CLI win than the `Clause.problem_ternary` attempt did. A scratch core branch that added immutable `Clause.problem_ternary` metadata and a dedicated original-ternary fast branch in `propagate()` did improve the 12-case SAT-heavy root-hit exact-CLI slice (`0.5028s -> 0.4952s`), but it still regressed the mixed nine-case exact-CLI hotspot slice from `32.7926s` to `34.1993s`, with `large/test_6.cnf` and `special/hard.cnf` both worse in both orders. So future immutable metadata work should distinguish “replace one hot shape test” from “hoist a whole clause family into its own branch”; the former can still win, but the latter needs a much stronger dense-case story.
62. Treat lazy heap-based branch-frontier maintenance as another branching dead end unless a future design avoids both heavy activity-update churn and backtrack reinsertion churn. A scratch branch that replaced the read-only `pick_branch_literal()` scan with a lazy `heapq` frontier regressed the mixed nine-case exact-CLI hotspot slice from `38.5681s` to `55.0274s`, with catastrophic losses on `large/test_6.cnf` (`16.3472s -> 26.6500s` forward, `20.8493s -> 26.6526s` reverse) and `special/hard.cnf` (`13.3576s -> 19.0475s` forward, `13.0566s -> 18.9846s` reverse). So future branching work should stay skeptical of mutable active-frontier structures; the current read-only scan is still far safer than maintaining a heap of branch candidates in pure Python.
63. Treat the current promoted `satsolver.py` wrapper as the stronger exact-CLI alternate-file baseline again. A fresh same-day A/B showed the SAT-heavy root-hit slice essentially tied (`0.5198s` for `satsolver.py` vs `0.5205s` for `satsolver_fast.py`) while the mixed nine-case exact-CLI hotspot slice still favored `satsolver.py` overall (`36.5697s` vs `36.9565s`), with the main reverse-order damage for `satsolver_fast.py` on `special/hard.cnf` and `large/test_6.cnf`. So future alternate-file work should start from current `satsolver.py` unless a materially different wrapper beats it on the mixed slice, not just on startup-heavy cases.
64. Treat the current narrow portfolio handoff as still required exact-CLI behavior unless a future single-case accelerator can replace it cleanly. A scratch main-wrapper variant that disabled portfolio entirely did improve the 12-case SAT-heavy exact-CLI slice (`0.4724s -> 0.4478s`), but it regressed the mixed nine-case exact-CLI hotspot slice badly (`30.3681s -> 34.0072s`) because the one remaining portfolio-triggered case, `large/test_8.cnf`, blew up from `0.3372s -> 3.8820s` forward and `0.3526s -> 4.1060s` reverse. So future wrapper work should not assume that “only one case still uses portfolio” means the fork path is now disposable; a replacement for that case has to beat the current handoff on the broad mixed gate, not just on the startup-heavy slice.
65. Treat “replace the one remaining portfolio call with serial phase-bias solve” as another too-small exact-CLI wrapper idea unless a future branch adds a materially stronger fallback story. A scratch wrapper that replaced the current portfolio handoff with `solve_cnf_fast_serial(..., seed_phase_bias=True)` did improve `large/test_8.cnf` directly and improved the mixed nine-case exact-CLI hotspot slice (`30.7417s -> 30.3299s`), but the same-day repeat-aware exact-CLI 59-case suite still came back slightly worse overall (`35.2571s -> 35.2674`). So future portfolio work should not assume that a one-case shortcut is enough; it still has to survive the broad repeat-aware suite, not just the mixed slice.
66. Treat “skip `minimize_learnt()` for ternary learnt clauses” as another conflict-analysis dead end unless a future branch shows a materially stronger dense UNSAT upside. A scratch core branch that changed the early-return guard in `minimize_learnt()` from `len(learnt) <= 2` to `<= 3` regressed the mixed nine-case exact-CLI hotspot slice from `30.4068s` to `30.5538s`. `special/hard.cnf` improved in both orders, but `large/test_6.cnf` and `large/test_10.cnf` both regressed in both orders, so future conflict-analysis work should not assume that skipping minimization on already-small learnt clauses is free.
67. Treat “move literal truth into positive watch-index space” as another propagation micro-optimization dead end unless a future branch removes more than indexing style. A scratch core rewrite that stored `literal_values` in watch-index space and routed `literal_value()`, `enqueue()`, `backtrack()`, and `propagate()` through that mapping regressed the mixed nine-case exact-CLI hotspot slice from `31.0231s` to `32.7078s`, with broad dense-case damage on `large/test_6.cnf`, `special/hard.cnf`, and `large/test_10.cnf`. So future propagation work should not assume that replacing signed negative indexing with another truth-cache encoding is enough by itself.
68. Treat the zero-activity `phase_bias` fallback in `pick_branch_literal()` as removable baggage on the current mainline. A kept core change that removed `if self.activity[best_variable] == 0.0: positive = self.phase_bias[best_variable] >= 0` and always used `saved_phase[best_variable]` improved the mixed nine-case exact-CLI hotspot slice from `32.6520s` to `32.5010s` and improved the same-day repeat-aware exact-CLI 59-case suite from `38.1276s` to `37.8494s`, still `59/59` correct. That lines up with the earlier branch-frontier profiling that found zero-activity branch choices absent on the main hotspot cases. So future branching work should not preserve the old phase-bias fallback out of caution, but it should still stay skeptical of heavier mutable-frontier or heap-based branch-picking rewrites.
69. Treat propagated `saved_phase` updates as essential current behavior unless a future branch brings a materially stronger replacement for the lost search guidance. A scratch core branch that removed `saved_phase[variable] = ...` writes from the three inlined propagated-unit assignment sites in `propagate()` regressed the mixed nine-case exact-CLI hotspot slice from `30.5205s` to `56.9678s`, with `large/test_8.cnf` exploding from about `0.34s` to about `25s` in both orders while still staying checker-valid. So future propagation work should not assume that trimming phase-saving writes is a harmless hot-path cleanup; for the current solver, propagated phase saving is still a major part of the search behavior.
70. Treat lazy `phase_bias` recomputation as another startup/ingest dead end unless a future branch removes substantially more clause-ingest bookkeeping than this one did. A scratch branch that stopped updating `phase_bias` during `observe_clause()` and recomputed it only in the rare `seed_phase_bias=True` path still regressed the 12-case SAT-heavy exact-CLI root-hit slice from `0.4780s` to `0.4922s`, with broad small/medium SAT-case losses. So future exact-CLI wrapper or clause-ingest work should not assume that eager `phase_bias` maintenance is recoverable overhead by itself.
71. Treat direct `literal_var[...]` substitution for hot `abs(...)` calls in conflict analysis as another pure-overhead dead end unless a future branch removes more actual analysis work than this one did. A scratch core branch that rewrote `analyze()`, `minimize_learnt()`, and `prepare_learnt_clause()` to use the solver's precomputed literal-to-variable lookup instead of `abs(...)` still regressed the mixed nine-case exact-CLI hotspot slice from `30.3393s` to `30.9773s`, with `large/test_6.cnf` worse in both orders (`15.2590s -> 15.6328s` forward, `14.5444s -> 15.4005s` reverse). So future conflict-analysis work should not assume that a profiler-visible `abs` cost can be recovered by swapping it for another primitive without cutting more real reasoning work.
72. Treat “keep ternary learnt clauses with `LBD <= 3`” as another learnt-database dead end unless a future branch comes with a materially stronger SAT-family safeguard. A scratch core branch that broadened `reduce_database()` retention to preserve ternary learnt clauses through `LBD == 3` blew up the mixed nine-case exact-CLI hotspot slice from `31.1445s` to `54.9538s`, almost entirely because `large/test_8.cnf` exploded from `0.3193s -> 25.6356s` forward and `0.3519s -> 24.9397s` reverse. `special/hard.cnf` and `large/test_10.cnf` did improve, but not remotely enough to repay the SAT-family collapse. So future learnt-database work should stay skeptical of retention broadening for small ternary learnt clauses.
73. Treat selective clause-activity bump skipping as another likely learnt-database dead end unless a future branch brings a materially stronger dense-UNSAT gain. A scratch core branch that changed `analyze()` to skip `bump_clause_activity()` for learnt clauses that were already effectively always kept (binaries and `LBD <= 2`) still regressed the mixed nine-case exact-CLI hotspot slice from `32.6181s` to `32.8545s`, with the decisive loss on `large/test_6.cnf` reverse (`16.0836s -> 17.4529s`). `special/hard.cnf` improved in both orders, but not enough to recover the dense-case loss. So future learnt-database work should not assume that trimming activity bumps on “safe” learnt clauses is free overhead to harvest.
74. Treat “defer watcher-list shrink to one tail delete per list” as another propagation micro-optimization dead end unless a future branch removes more than current list-shrink overhead. A scratch core branch that replaced per-removal watcher `pop()` calls with a logical end pointer plus one final `del watchers[watchers_end:]` still regressed the mixed nine-case exact-CLI hotspot slice from `30.6616s` to `31.6370s`, with both `special/hard.cnf` and `large/test_6.cnf` worse in both orders. So future propagation work should not assume that the watcher-pop hotspot is recoverable just by changing shrink mechanics while keeping the same traversal and relocation work.
75. Treat tiny read-only branch-scan cleanup as a still-live branching lane, distinct from the already-rejected mutable frontier ideas. A kept core change that hoisted `self.values`, `self.activity`, `self.saved_phase`, and `self.num_vars` into locals inside `pick_branch_literal()` improved the fresh `large/test_6.cnf` `cProfile` `pick_branch_literal()` cost from `1.626s` to `1.388s`, improved the mixed nine-case exact-CLI hotspot slice from `31.0625s` to `30.9412s`, and improved the same-day repeat-aware exact-CLI 35-case suite from `32.7030s` to `31.6015s`, all still correct. So future branching work can still consider very small same-search scan cleanups, but it should keep rejecting heaps, active-frontier structures, and other mutable branch-state machinery unless a materially stronger broad-suite signal appears.
76. Treat inline `solve()` helper cleanup around activity decay as another hotspot-only mirage unless a future branch survives the repeat-aware exact-CLI suite. A scratch core branch that replaced `self.decay_var_activity()` and `self.decay_clause_activity()` with direct assignments inside `solve()` improved the mixed nine-case exact-CLI hotspot slice from `30.7973s` to `30.4203s`, largely by helping `large/test_6.cnf`, but still regressed the repeat-aware exact-CLI 35-case suite from `33.4842s` to `33.6295`. So future helper-boundary cleanup in the main conflict loop should demand the same broad-suite confirmation as `analyze()`/wrapper cleanups, not just a positive hotspot slice.
77. Treat broad local method binding in `solve()` as another helper-boundary dead end unless a future branch shows a materially different broad-suite gain. A scratch core branch that bound `propagate`, `analyze`, `add_learnt_clause`, `enqueue`, `reduce_database`, `pick_branch_literal`, the decay helpers, and `trail_limits` into locals once per `solve()` call regressed the mixed nine-case exact-CLI hotspot slice from `29.4850s` to `31.0535s`, with the worst loss on reverse-order `large/test_6.cnf` (`14.6849s -> 16.9758s`). So future `solve()`-loop cleanup should not assume that the kept `pick_branch_literal()` local-alias win generalizes to broad method binding at the loop level.
78. Treat local builtin binding around conflict-analysis `abs(...)` calls as another low-level dead end unless a future branch removes more real reasoning work than primitive lookup overhead alone. A scratch core branch that bound `abs` into locals inside `analyze()`, `minimize_learnt()`, and `prepare_learnt_clause()` still regressed the mixed nine-case exact-CLI hotspot slice from `31.4277s` to `32.1594s`, with `large/test_6.cnf` (`16.3400s -> 16.5961s` forward, `15.3030s -> 15.4358s` reverse) and `special/hard.cnf` (`10.4151s -> 10.8689s` forward, `9.6581s -> 10.3215s` reverse) worse in both orders. So future conflict-analysis work should not assume that the visible `abs` cost in the profiler can be recovered just by binding the builtin locally; the next real win still needs to simplify more actual analysis work.
79. Treat fixed-size learnt-clause preparation fast paths as another likely conflict-analysis dead end unless a future branch comes with materially stronger broad-suite evidence. A scratch core branch that special-cased `prepare_learnt_clause()` for binary and ternary learnt clauses regressed the mixed nine-case exact-CLI hotspot slice from `32.1394s` to `34.0749s`, with the main damage on `large/test_6.cnf` (`15.9523s -> 18.1880s` forward, `14.9877s -> 16.9680s` reverse). So future conflict-analysis work should not assume that short learnt-clause postprocessing is a safe target for hand-specialized fast paths; the next real win still needs a stronger structural simplification than “skip the generic loop for tiny learnt clauses.”
80. Treat flat binary implication payloads as another likely propagation micro-optimization dead end unless a future branch removes more than tuple-unpacking overhead alone. A scratch core branch that stored `binary_implications` as alternating `implied_literal, clause_id` integers and iterated them by step `2` regressed the mixed nine-case exact-CLI hotspot slice from `35.0798s` to `36.0096s`: forward order improved slightly (`35.8088s -> 35.5085s`), but reverse order lost much more strongly (`34.3507s -> 36.5107s`), with the main damage on `large/test_6.cnf` (`16.9915s -> 18.9111s`) and `large/test_10.cnf` (`2.2495s -> 2.4885s`). So future propagation work should not assume that simply flattening binary implication payloads is enough; the next real win still needs to remove larger watcher/ternary traversal cost.
81. Treat one-pass full-tail `backtrack()` rewriting as another hotspot-only mirage unless a future branch survives the repeat-aware exact-CLI suite. A scratch core branch that computed the backtrack cut point once, cleared the whole trail tail in one pass, and sliced `trail_limits` once improved the mixed nine-case exact-CLI hotspot slice from `38.9067s` to `38.1503s`, largely by helping `large/test_6.cnf` in both orders. But it still regressed the repeat-aware exact-CLI 35-case suite from `39.2286s` to `39.7958`, with the broad loss driven mainly by `special/hard.cnf` (`11.8325s -> 12.8074s`) and `medium/test_4.cnf` (`2.4703s -> 2.6679s`). So future backtrack cleanup should demand the same broad-suite confirmation as other hotspot-looking wins.
82. Treat “retune the second portfolio worker with a more aggressive restart schedule” as another portfolio dead end unless a future design comes with materially stronger one-case evidence. A scratch exact-CLI wrapper that kept the current default worker but changed the phase-biased worker to `seed_phase_bias=True, restart_base=32` blew the gated case `large/test_8.cnf` up from `0.4129s` to `5.5657s` on the two-order average while still staying SAT-correct. So future portfolio work should not assume that the current gate is narrow enough to make restart retuning safe; the remaining protected case is still highly restart-sensitive.
83. Treat “merge the standalone root propagation into the main `solve()` loop” as another startup-only mirage unless a future cleanup also helps the dense exact-CLI gate. A scratch core branch that removed the pre-loop `propagate()` call from `solve()` did improve the 12-case SAT-heavy exact-CLI root-hit slice slightly from `0.5092s` to `0.5010s`, but it still regressed the mixed nine-case exact-CLI hotspot slice from `36.2592s` to `36.5577s`, with `large/test_6.cnf` worse in both orders (`18.3900s -> 18.8554s`, `18.1681s -> 18.6613s`). So future outer-loop cleanup should not assume that deleting one apparently redundant startup call is a free win; the next real speed gain still needs to survive the dense mixed gate, not just the startup-heavy slice.
84. Treat `reduce_database()` bookkeeping and locked-clause micro-optimizations as even lower priority unless a future branch changes clause selection or search policy itself. Fresh profiler data shows that on the main hotspots, reductions only carry about `22.73..33.07` locked clauses against `315.26..469.92` candidates while deleting about half of those candidates each time (`large/test_6.cnf`: `23.94` locked vs `447.42` candidates; `special/hard.cnf`: `24.69` vs `469.92`; `medium/test_4.cnf`: `22.73` vs `320.55`; `large/test_8.cnf`: `33.07` vs `315.26`). So future `reduce_database()` work should target classifier/search effects, not more locked-set or candidate-bookkeeping cleanup.
85. Treat raw restart-cadence profiling as too weak by itself to reopen restart tuning. Fresh profiler data shows the dense cases already restart every `166.82..226.40` conflicts at average decision level `8.21..16.30` and average trail `32.77..51.83`, and the repo already has multiple failed restart-policy retunes. So future restart work should require a materially stronger family-specific classifier than generic cadence tweaks.
86. Treat “same logic, different loop primitive” in `pick_branch_literal()` as another branching micro-optimization dead end unless a future branch removes more than loop-shape overhead. A scratch core branch that replaced the current `for range(...)` scan with a manual `while variable <= num_vars` scan regressed the mixed nine-case exact-CLI hotspot slice from `28.0431s` to `29.6058s`, with `large/test_6.cnf`, `special/hard.cnf`, and `medium/test_4.cnf` all worse in both orders. So future branching work should distinguish the kept local-alias cleanup from dead loop-shape churn; the next real win is still more likely in `propagate()` or conflict analysis.
87. Treat “leave the false watch in place when a ternary replacement candidate is already `TRUE`” as another strong original-ternary dead end. A scratch core branch that stopped relocating ternary clauses on the `candidate_value == TRUE` path and simply kept the clause satisfied in place blew the mixed nine-case exact-CLI hotspot slice up from `34.2608s` to `55.5632s`, with `large/test_8.cnf` exploding from about `0.35s` to about `12s` and `large/test_6.cnf` plus `special/hard.cnf` much worse in both orders. So future original-ternary work should not assume that minority satisfied-candidate relocations are easy churn to remove; the dominant relocation/unit path is still the right target.
88. Treat bundled cross-cutting core cleanups as unproven unless they survive the repeat-aware exact-CLI suite one change at a time. A branch that combined integer-only `reason` sentinel storage with a redundant no-replacement-tail cleanup in `propagate()` looked slightly positive on the mixed nine-case exact-CLI hotspot slice (`38.0602s -> 37.6673s`) but regressed the repeat-aware exact-CLI 35-case suite from `40.3613s` to `43.3099s`, with `large/test_6.cnf`, `special/hard.cnf`, `large/test_10.cnf`, and `large/test_8.cnf` all worse. So future cross-cutting cleanup work should isolate those lanes one at a time and still require broad-suite confirmation.
89. Treat isolated no-replacement-tail branch cleanup in `propagate()` as another hotspot-only mirage unless a future branch brings materially stronger broad-suite evidence. A scratch core branch that changed the two no-replacement tails from `if other_value == FALSE ... if other_value == UNASSIGNED ...` to `if other_value == FALSE ... else ...` improved the mixed nine-case exact-CLI hotspot slice from `32.5969s` to `31.1921s`, led by `large/test_6.cnf` and `special/hard.cnf` in both orders. But it still regressed the repeat-aware exact-CLI 35-case suite from `33.6532s` to `38.5035`, with `large/test_6.cnf`, `special/hard.cnf`, `medium/test_4.cnf`, and `large/test_10.cnf` all worse. So future branch-shape cleanup in the watched-clause tails should still require broad-suite confirmation even when the local hotspot A/B looks convincingly positive.
90. Treat watched-clause no-replacement-tail branch-shape cleanup as a likely dead lane unless a future branch changes something materially larger than the branch itself. A rerun of the isolated `if other_value == FALSE ... else ...` cleanup on the current retained baseline reproduced the same pattern: strong mixed exact-CLI hotspot improvement (`32.5969s -> 31.1921s`) followed by a hard repeat-aware exact-CLI 35-case suite regression (`33.6532s -> 38.5035`). So future work should stop treating that branch shape as a live micro-optimization target by itself and go back to more structural `propagate()` or conflict-analysis changes.
91. Treat array/bytearray-backed hot solver state as another pure-Python core-layout dead end unless a future branch removes much more work than a container swap. A scratch core branch that moved `literal_values`, `literal_var`, `literal_sign`, `literal_watch_index`, `negated_watch_index`, `values`, `level`, `phase_bias`, `saved_phase`, `seen`, and `lbd_marks` onto stdlib typed arrays regressed the mixed nine-case exact-CLI hotspot slice from `25.5674s` to `31.1749s`, with the biggest losses on `large/test_6.cnf` (`11.9928s -> 16.2617s` forward, `13.8218s -> 16.3599s` reverse), `special/hard.cnf`, `medium/test_4.cnf`, and `large/test_10.cnf`. So future data-layout work should stay focused on removing watcher/conflict-analysis work, not just storing the same integers in denser containers.
92. Treat isolated integer-only `reason` sentinel storage as another hotspot-only mirage unless a future branch also removes materially more conflict-analysis or reduction work. A scratch core branch that changed `reason` from `None`/clause-id to `NO_REASON = -1` was mildly positive on the mixed nine-case exact-CLI hotspot slice (`24.3347s -> 24.1806s`), but still regressed the repeat-aware exact-CLI 35-case suite from `25.4875s` to `25.9045s`, with `large/test_6.cnf`, `special/hard.cnf`, `medium/test_4.cnf`, and `large/test_8.cnf` all worse. So future cleanup work should stop treating integer-only `reason` storage as a live lane by itself and go back to more structural `propagate()` or conflict-analysis changes.
93. Treat global phase-bias seeding on the main exact-CLI fast path as another phase-policy dead end unless a future branch comes with a much stronger classifier than “turn it on everywhere.” A scratch wrapper branch that changed the default `solve_cnf()` path to call `solve_cnf_fast_serial(..., seed_phase_bias=True)` regressed the mixed nine-case exact-CLI hotspot slice from `31.7103s` to `37.6381s`, with especially bad losses on `special/hard.cnf` (`9.6094s -> 13.4149s` forward, `9.9988s -> 14.9598s` reverse), `large/test_6.cnf` reverse (`16.6386s -> 19.2728s`), and `medium/test_4.cnf`. So future phase work should keep treating phase-bias seeding as a narrow specialist heuristic rather than a broad default-policy improvement.
94. Treat portfolio-worker wrapper churn as another nearly-exhausted one-case lane unless a future branch brings a materially stronger `large/test_8.cnf` win than this. A scratch branch that kept the current narrow portfolio gate but switched both workers from `solve_cnf_serial()` to `solve_cnf_fast_serial()` still regressed the repeat-2 exact-CLI `large/test_8.cnf` two-order average from `0.2924s` to `0.2950s` (`0.2772s -> 0.3008s` forward, `0.3075s -> 0.2892s` reverse). So future portfolio work should demand a clearly positive one-case exact-CLI signal before spending more mixed-slice or full-suite benchmarking budget on worker-plumbing tweaks.
95. Treat binary-implication deleted-check removal as another micro-cleanup dead end unless a future branch removes materially more binary-path work than one guard branch. A scratch core branch that removed the `clause.deleted` check from the binary implication loop in `propagate()` looked semantically safe because `reduce_database()` never deletes binary clauses, but it still regressed the mixed nine-case exact-CLI hotspot slice from `32.4733s` to `32.7124s`. Forward order got worse on `special/hard.cnf`, `medium/test_4.cnf`, `medium/test_3.cnf`, and `large/test_6.cnf`, while reverse order improved on some of the same cases but not enough to recover the loss. So future binary-path work should not assume that an apparently redundant deleted-clause guard is meaningful by itself; the next real gain still needs to remove more actual propagation work than one branch and one clause lookup.
96. Use `satsolver_pysat.py` as an external upper-bound comparison tool, not as a submission replacement. The new PySAT-backed wrapper solved the mixed exact-CLI nine-case hotspot slice in `2.4796s` versus `32.8031s` for the retained main wrapper, and the single-run exact-CLI 35-case suite in `8.6607s` versus `37.4322s`, both `35/35` correct. But it lost badly on the repo's structural presolver cases like `special/pigeonhole.cnf` and `special/tseitin.cnf`. So future pure-Python optimization work should read this as “the dense UNSAT search core is still the real gap,” while keeping the handcrafted structural fast exits.
