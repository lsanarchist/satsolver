schema_version: 1
project: satsolver
mode: autonomous_queue
current_phase: 2
current_task_id: null
last_completed_task_id: perf-057
repo_health: green_verified
working_assumptions:
  - AGENT.md plus .agent/* are the authoritative autonomous control plane.
  - PLANS.md remains the durable narrative log for multi-step autonomous work, but not the task-selection source of truth.
  - Fast verification defaults to python tools/codex_verify.py, which now includes tools/agent_queue_check.py and satsolver_fast.py smoke checks.
  - External libraries may be used only as short-lived research references; retained solver code and default verification remain standard-library only.
blockers: []
recent_files:
  - PLANS.md
  - .agent/STATE.yaml
  - .agent/TASK_QUEUE.yaml
  - .agent/HANDOFF.md
  - .agent/WORKLOG.md
  - tools/profile_solver.py
  - tests/test_profile_solver.py
next_task_hint: perf-058
notes:
  - The repo previously had AGENTS.md plus PLANS.md guidance but no repo-local queue control plane.
  - Routine verification now checks queue consistency before unit tests and solver smoke cases.
  - Shared DIMACS parsing and result-writing now live in satsolver_io.py for the thin wrappers.
  - Routine verification now also smoke-tests the standard-library alternate wrapper path via satsolver_fast.py.
  - The retained portfolio thresholds still gate only large/test_8.cnf after same-day revalidation; no broadened threshold change was kept.
  - The queue now runs as a rolling native-only optimization program rather than as a single open-ended performance task.
  - The refreshed exact-CLI seven-case hotspot slice is large/test_6.cnf, special/hard.cnf, large/test_10.cnf, medium/test_4.cnf, medium/test_3.cnf, satlib_more/uuf150-01.cnf, and large/test_8.cnf.
  - Perf-004 kept no solver change: a coarse len>=10 learnt-clause penalty inside reduce_database() regressed the seven-case exact-CLI gate catastrophically, with the worst damage on large/test_8.cnf.
  - Perf-005 kept no solver change: a low-LBD early-restart trigger regressed the seven-case exact-CLI gate broadly and still inflated large/test_8.cnf badly.
  - Perf-006 kept no solver change: fresh startup measurements show the remaining exact-CLI floor is now dominated by interpreter startup, with only a few milliseconds of repo-local wrapper cost left on tiny cases.
  - Perf-007 kept no solver change: refreshed PySAT gap analysis showed the retained solver still wins the structural fast-exit families but trails a mature external backend by roughly 20x on the dense seven-case UNSAT-heavy hotspot slice.
  - Perf-008 kept no solver change: a true problem-ternary watcher split preserved the structural fast-exit families but changed the dense UNSAT search path badly enough to blow up large/test_6.cnf from 59,201 to 81,161 conflicts.
  - Perf-009 kept no solver change: even skipping minimization scans only for learnt 10+-literal reasons regressed the seven-case exact-CLI gate badly, including a large/test_8.cnf blow-up from about 0.38s to about 5.07s.
  - Perf-010 kept no solver change: even a same-content ternary-first branch reorder inside minimize_learnt() regressed the seven-case exact-CLI gate overall, so future conflict-analysis bookkeeping should move away from reason-size branch-order tweaks.
  - Perf-011 kept no solver change: peeling the first two literals out of prepare_learnt_clause() regressed the seven-case exact-CLI gate overall, so future learnt-finalization work should move away from pure loop-shape cleanup.
  - Perf-012 kept a solver change: computing backtrack/LBD metadata during post-minimization compaction improved the seven-case exact-CLI slice and the repeat-aware 59-case exact-CLI suite while keeping the dense hard-case decision/conflict counters unchanged.
  - Perf-013 kept no solver change: the retained post-perf-012 profile still shows propagate() dominating end-to-end time, with original problem-ternary relocation and unit handling remaining the biggest concentrated surface on the dense UNSAT hotspots.
  - Perf-014 kept a solver change: taking the overwhelmingly common unit path directly in the ternary candidate-false tail improved both the seven-case exact-CLI hotspot slice and the same-day repeat-aware 59-case exact-CLI suite while keeping the dense hard-case decision/conflict counters unchanged.
  - Perf-015 kept no solver change: the retained post-perf-014 profile still shows propagate() dominating end-to-end time, with original problem-ternary relocation still the larger remaining surface than units on the dense UNSAT hotspots.
  - Perf-016 kept no solver change: splitting the dominant ternary relocation branch into explicit `candidate=UNASSIGNED` and `candidate=TRUE` cases preserved the dense hard-case decision/conflict counters but still regressed the seven-case exact-CLI gate, so future relocation work should remove bookkeeping rather than add more candidate-state branching.
  - Perf-017 kept a solver change: direct use of `candidate_literal` on the ternary relocation path improved both the seven-case exact-CLI hotspot slice and the repeat-aware 59-case exact-CLI suite while keeping the dense hard-case decision/conflict counters unchanged.
  - Perf-018 kept no solver change: the retained post-perf-017 profile still shows propagate() dominating end-to-end time, with original problem-ternary relocation still primary and learnt-large relocation still the next secondary watcher-churn bucket on the dense UNSAT hotspots.
  - Perf-019 kept no solver change: directly rewriting successful learnt-large relocation swaps won the focused seven-case and structural guardrails but regressed the repeat-aware 59-case exact-CLI suite, so future learnt-large work needs a broader exact-CLI guard slice before another keep attempt.
  - Perf-020 kept no solver change: the perf-019 broad-suite regression came almost entirely from repeat-aware movement inside the existing focused seven-case slice, with only a small secondary satlib_more tail outside it, so future learnt-large work should keep the seven-case slice as primary and add a compact satlib_more guard slice before the full-suite keep gate.
  - Perf-021 kept no solver change: reordering the learnt-large no-replacement tail to favor the common unit-or-satisfied path preserved dense hard-case search counters but still regressed both the focused seven-case slice and the supplemental satlib_more slice, so future learnt-large work should move away from failure-tail branch-order tweaks and profile the supplemental satlib_more cases directly before the next candidate.
  - Perf-022 kept no solver change: the supplemental satlib_more slice split into two profiler families, with jnh10/jnh1 dominated by problem-large relocation while uuf125-010/uf125-01/uf125-010 carry the real learnt-large traffic; future learnt-large candidates should target the latter and treat the former as problem-large guardrails.
  - Perf-023 kept no solver change: a selective learnt-large success-path rewrite for long or deep probes improved the supplemental satlib_more slice overall, led by uuf125-010, but still regressed the primary seven-case gate, so the next follow-up should narrow further to the truly overlapping long-and-deep subset instead of keeping the broader OR-gated rewrite.
  - Perf-024 kept no solver change: narrowing the rewrite to the overlapping len10+ and step-3+ learnt-large success lane regressed both the primary seven-case gate and the supplemental slice, so the next follow-up should measure the remaining non-overlap buckets before trying another solver-core success-path edit.
  - Perf-025 kept no solver change: refreshed learnt-large success-bucket counters showed the surviving perf-023 non-overlap signal is the short-but-deep sub-10-literal step-3+ lane across uuf125-010 and the uf* pair, while jnh10 and jnh1 remain low-volume problem-large guardrails.
  - Perf-026 kept no solver change: applying the direct watched-slot rewrite across the whole short-but-deep sub-10 step-3+ learnt-large lane was too wide, barely moving the focused seven-case slice while regressing the supplemental satlib_more guard slice, so the next follow-up should split that lane by exact depth before another solver-core edit.
  - Perf-027 kept no solver change: the short-but-deep lane is dominated by sub-10 step-3/4 successes across uuf125-010 and the uf* pair, while step-5+ successes are a much smaller tail and should not define the next solver-core candidate.
  - Perf-028 kept no solver change: even narrowing the direct watched-slot rewrite to sub-10 step-3/4 learnt-large successes left the supplemental slice only marginally positive and still regressed the primary seven-case gate, so the next follow-up should split step-3/4 by exact step before another solver-core experiment.
  - Perf-029 kept no solver change: exact-step profiling shows the surviving sub-10 step-3/4 signal is led by exact step-3 on the real target trio, while step-4 survives mainly as a guardrail tail, so the next solver-core candidate should target only the step-3 lane.
  - Perf-030 kept no solver change: even narrowing the direct watched-slot rewrite to the exact sub-10 step-3 learnt-large lane regressed both the primary seven-case and supplemental guard slices, so the next follow-up should re-profile exact step-3 traffic on the focused hotspot cases before another solver-core candidate.
  - Perf-031 kept no solver change: exact sub-10 step-3 learnt-large traffic on the focused seven-case slice is dominated by special/hard.cnf and large/test_6.cnf, which together account for about 78.9 percent of the total, so the next solver-core candidate should target those dense UNSAT anchors first and treat the rest of the slice as guardrails.
  - Perf-032 kept no solver change: skipping exact step-3 source-list self-assignment on the dense anchors stayed same-search but still regressed the focused and supplemental guard slices, so the next follow-up should profile source-pop tail-position behavior on special/hard.cnf and large/test_6.cnf before another solver-core edit.
  - Perf-033 kept no solver change: exact dense-anchor step-3 source-pop tail-position profiling showed that last-slot self-assignment cases are only about 18 to 19 percent of the lane on both anchors, so the next dense-UNSAT solver-core experiment should target the dominant non-last overwrite path instead.
  - Perf-034 kept no solver change: even restricting the pop-first source-pop rewrite to the dominant exact step-3 non-last overwrite lane stayed same-search but still regressed the dense anchors, focused seven-case gate, and supplemental slice, so the next follow-up should profile where the overwrite source index actually lands before another solver-core edit.
  - Perf-035 kept no solver change: dense-anchor exact step-3 non-last overwrites skew deeper in the watcher list on both anchors, with only about one third of overwrite traffic in the first two slots and about two thirds at index 2 or deeper, so the next bounded rewrite should target the deeper overwrite tail instead of the whole overwrite lane.
  - Perf-036 kept no solver change: even narrowing the pop-first rewrite to the deeper exact step-3 overwrite aggregate at watcher-list index 2 or deeper still regressed the dense anchors and focused seven-case gate despite a positive supplemental satlib_more slice, so the next follow-up should split that deeper lane by exact source index before another solver-core edit.
  - Perf-037 kept no solver change: exact deep-overwrite profiling now shows that the surviving exact step-3 deep lane is dominated by source index 3+ rather than exact index 2 on both the dense anchors and the real supplemental target trio, so the next bounded solver-core candidate should target only the index-3-plus tail.
  - Perf-038 kept no solver change: even narrowing the pop-first rewrite to the exact step-3 deep-overwrite index-3-plus aggregate still regressed the dense anchors, focused seven-case gate, and supplemental slice, so the next follow-up should split that index-3-plus tail by exact source index before another solver-core edit.
  - Perf-039 kept no solver change: exact index-3-plus deep-overwrite profiling now shows that the surviving step-3 deep tail is dominated by source index 4+ rather than exact index 3 on both the dense anchors and the real supplemental target trio, so the next bounded solver-core candidate should target only the index-4-plus tail.
  - Perf-040 kept no solver change: even narrowing the pop-first rewrite to the exact step-3 deep-overwrite index-4-plus aggregate stayed same-search and improved the primary hotspot gates, but it still regressed the supplemental satlib_more slice and the repeat-aware exact-CLI full suite, so the next follow-up should split that index-4-plus tail by exact source index before another solver-core edit.
  - Perf-041 kept no solver change: exact index-4-plus deep-overwrite profiling now shows that the surviving step-3 deep tail is dominated by source index 5+ rather than exact index 4 on both the dense anchors and the real supplemental target trio, so the next bounded solver-core candidate should target only the index-5-plus tail.
  - Perf-042 kept no solver change: even narrowing the pop-first rewrite to the exact step-3 deep-overwrite index-5-plus aggregate still regressed the dense anchors, focused seven-case gate, and supplemental slice, so the next follow-up should split that index-5-plus tail by exact source index before another solver-core edit.
  - Perf-043 kept no solver change: exact index-5-plus deep-overwrite profiling now shows that the surviving step-3 deep tail is dominated by exact source index 6+ rather than exact index 5 on both the dense anchors and the real supplemental target trio, so the next bounded solver-core candidate should target only the index-6-plus tail.
  - Perf-044 kept no solver change: even narrowing the pop-first rewrite to the exact step-3 deep-overwrite index-6-plus aggregate kept the same dense-anchor search counts and won the focused/supplemental slices, but it still regressed the repeat-aware exact-CLI full suite, so the next follow-up should split that index-6-plus tail by exact source index before another solver-core edit.
  - Perf-045 kept no solver change: exact index-6-plus deep-overwrite profiling now shows that the surviving step-3 deep tail is dominated by exact source index 7+ rather than exact index 6 on both the dense anchors and the real supplemental target trio, so the next bounded solver-core candidate should target only the index-7-plus tail.
  - Perf-046 kept no solver change: even narrowing the pop-first rewrite to the exact step-3 deep-overwrite index-7-plus aggregate still regressed the dense anchors, focused seven-case gate, and supplemental slice, so the next follow-up should split that index-7-plus tail by exact source index before another solver-core edit.
  - Perf-047 kept no solver change: exact index-7-plus deep-overwrite profiling now shows that the surviving step-3 deep tail is dominated by exact source index 8+ rather than exact index 7 on both the dense anchors and the real supplemental target trio, so the next bounded solver-core candidate should target only the index-8-plus tail.
  - Perf-048 kept no solver change: even narrowing the pop-first rewrite to the exact step-3 deep-overwrite index-8-plus aggregate still regressed the dense anchors and focused seven-case gate despite a positive supplemental satlib_more slice, so the next follow-up should split that index-8-plus tail by exact source index before another solver-core edit.
  - Perf-049 kept no solver change: exact index-8-plus deep-overwrite profiling now shows that the surviving step-3 deep tail is dominated by exact source index 9+ rather than exact index 8 on both the dense anchors and the real supplemental target trio, so the next bounded solver-core candidate should target only the index-9-plus tail.
  - Perf-050 kept no solver change: even narrowing the pop-first rewrite to the exact step-3 deep-overwrite index-9-plus aggregate left the dense anchors slightly negative and the focused seven-case slice essentially flat while the supplemental satlib_more slice regressed clearly, so the next follow-up should split the index-9-plus tail by exact source index before another solver-core edit.
  - Perf-051 kept no solver change: exact index-9-plus deep-overwrite profiling now shows that the surviving step-3 deep tail is dominated by exact source index 10+ rather than exact index 9 on both the dense anchors and the real supplemental target trio, so the next bounded solver-core candidate should target only the index-10-plus tail.
  - Perf-052 kept no solver change: even narrowing the pop-first rewrite to the exact step-3 deep-overwrite index-10-plus aggregate improved the dense anchors slightly and stayed nearly flat on the supplemental slice, but it still regressed the focused seven-case gate, so the next follow-up should split that index-10-plus tail by exact source index before another solver-core edit.
  - Perf-053 kept no solver change: exact index-10-plus deep-overwrite profiling now shows that the surviving step-3 deep tail is dominated by exact source index 11+ rather than exact index 10 on both the dense anchors and the real supplemental target trio, so the next bounded solver-core candidate should target only the index-11-plus tail.
  - Perf-054 kept no solver change: even narrowing the pop-first rewrite to the exact step-3 deep-overwrite index-11-plus aggregate improved the dense anchors and the supplemental satlib_more slice, but it still regressed the focused seven-case gate, so the next follow-up should split that index-11-plus tail by exact source index before another solver-core edit.
  - Perf-055 kept no solver change: exact index-11-plus deep-overwrite profiling now shows that the surviving step-3 deep tail is dominated by exact source index 12+ rather than exact index 11 on both the dense anchors and the real supplemental target trio, so the next bounded solver-core candidate should target only the index-12-plus tail.
  - Perf-056 kept no solver change: even narrowing the pop-first rewrite to the exact step-3 deep-overwrite index-12-plus aggregate improved the supplemental satlib_more slice, but it still regressed the dense anchors and the focused seven-case gate, so the next follow-up should split that index-12-plus tail by exact source index before another solver-core edit.
  - Perf-057 kept no solver change: exact index-12-plus deep-overwrite profiling now shows that the surviving step-3 deep tail is dominated by exact source index 13+ rather than exact index 12 on both the dense anchors and the real supplemental target trio, so the next bounded solver-core candidate should target only the index-13-plus tail.
